# family-menu — 家庭每日食谱系统

为一个带幼儿的家庭生成周菜单与分批购物清单;家庭画像(人数、幼儿月龄、口味、地区)全部读 `data/config.json`,本文档不复述。
口味基调:咸鲜为主、吃辣,偏北方面食;时令按华东。

## 核心原则

1. **宝宝安全高于一切**:`data/baby_rules.json` 是硬规则库,任何改动必须由用户人工确认后才能提交,AI 不得即兴修改或绕过。菜谱的 baby_variant 生成后必须逐条对照 baby_rules 校验。
2. **一菜两做**:同一道菜,宝宝份在「加盐、加辣之前」盛出改造,不做两桌菜。做不到就标 `suitable: false` 并写明原因,宁严勿松。
3. **菜谱是决策单元,不是教程**:掌勺者会做家常菜,不写详细步骤;仅「宝宝重点」标签菜和生僻菜写 `steps_brief`。
4. **系统承担复杂性**:排菜、份量换算、采购批次分组、储存提示全部自动;用户操作只有三步:看菜单 → 换一换 → 拿清单。
5. **只标例外,不标常态**:全库 88% 的菜宝宝都能吃,逐道标「宝宝可吃」等于没标。页面上常显的是**宝宝不吃**的那几道(当餐要另想办法),能吃的收成一个可展开的小钮。同理「当季」只标限定月份的菜。

## 目录结构

```
family-menu/
├── CLAUDE.md
├── data/
│   ├── config.json        # 家庭画像、餐次模板、排菜约束、采购批次
│   ├── baby_rules.json    # 幼儿喂养硬规则(改动需用户人工确认)
│   ├── ingredients.json   # 食材库:时令月份 + 储存 + 宝宝适配
│   └── recipes.json       # 菜谱库(77 道)
├── app/
│   └── template.html      # 页面模板(唯一手改入口)
├── docs/
│   └── index.html         # 构建产物,单文件数据内嵌;GitHub Pages 发布目录
└── scripts/
    ├── build.js           # 校验 + 数据内嵌 + 构建
    └── artifact.js        # 把构建产物转成 artifact 预览页(.artifact/,不入库)
```

## 数据规范

- 所有 id 用拼音 snake_case(如 `dongua_shao_paigu`),全库唯一
- 份量字段 `qty_g` 一律为「全家一餐」的量(4 成人 + 娃),购物清单直接按批次累加
- `season_months` 用数字数组 [1-12],全年可用写 `"all"`
- 菜谱的 `main_ingredients` 中每个食材必须存在于 ingredients.json,不存在先补食材库再入菜谱
- 新增/修改字段:先改本文档和对应 `_schema` 说明,再改数据

## 菜谱量产流程(在 Claude Code 中执行)

目标 70 道:家常 30(约 20 荤、10 素/小菜)+ 补充荤菜 10 + 时令素菜 20 + 宝宝重点菜 5 + 面食主食 5。
排除项:臭鳜鱼等发酵重味菜。辣菜正常入库(标 spicy 等级)。
**状态:70 道基础目标已全部入库,后续按批追加(当前 76 道)**,新增走同一流程(见 SOP.md 进度台账)。
面食主食用 `category: 主食` + `meal_type: ["dinner"]`,面条本身带荤带素;排菜命中面食夜时不再另排荤菜。

流程:
1. 按 recipes.json 已有样例的 schema 批量生成,**每批 ≤ 10 道**
2. 每批生成后立即校验(build.js --validate 建成前用人工清单):
   - baby_variant 逐条对照 baby_rules.json 的 forbidden / texture 规则
   - spicy ≥ 1 的菜必须写明确的分菜时机(参考样例 `qingjiao_chao_rousi` 的写法);写不明确 → `suitable: false`
   - 主料在 ingredients.json 中可查
   - 荤菜 cook_time 与 config 的单餐 60 分钟预算兼容(炖菜标 prep_ahead)
3. 校验通过 → 呈现给用户确认 → 入库。宝宝相关字段有任何疑问一律从严处理。

## 排菜约束(算法硬约束)

- 早餐不上炒菜:饮品 + 主食 + 蛋,全部走 config 的轮换表,不占菜谱池;**主食轮换里杂粮 ≥ 50%**(7 天 4 天杂粮,杂粮日按 grain_rotation 换品种不重样);主食与饮品的忌撞组合写在 `breakfast.drink_conflicts`(稀饭 × 牛奶——两碗水),撞上时饮品顺延
- 面食仅晚餐,≤ 3 次/周;**午餐固定米饭,晚餐不吃米饭**——面食夜吃面,其余晚上走 `dinner.staple_rotation`(馒头/花卷)
- 汤 ≤ 2 次/周,插入项不占固定位;**有汤的晚上菜 + 汤合计 ≤ 2**(排了汤就不再排素菜)
- 同菜冷却 7 天;同主料每周 ≤ 2 次且须不同做法
- 单餐总耗时 ≤ 60 分钟
- 每周:猪肝 1-2 次(少量,宝宝补铁)、鱼 2-3 次(优先宝宝可用鱼种);区间从 baby_rules 的文案解析
  → 上限在选菜时由 `okHere()` 挡住,下限由 `enforceWeekly()` 生成后补位;补位优先于「同主料 ≤2 次」的软约束
- **不隔夜的鲜货**(ingredients 里 `same_day: true`:鲈鱼/虾/鲫鱼/猪肝)只能排在采购当天,这是硬约束;采购日读 config,不写死
- 叶菜按采购批次分配,不跨批
- 评分函数(已实现):硬约束过滤后对候选打分取最高,同分靠随机抖动保留变化
  → 时令分(限定月份的菜 +3,全年菜 0)+ 营养缺口填补(鱼/猪肝未达下限时 `5 × min(1, 还差几次 / 还剩几天)²`)+ 耗时匹配(当餐累计超 60 分钟 −6,可提前做的只 −1;≤30 分 +1)+ 食材复用(同一采购批次内已买过的生鲜主料 +1.5)− 重复惩罚(同主料每出现一次 −2,同一天撞主料再 −3)
- **填充顺序每次随机打乱**(只打乱先后,每天的 dayIdx 不变)。固定从周一开始填 = 每周都让周一先挑,全库分最高的那道菜会周周落在周一;冷却期判断因此要取 `Math.abs(dayIdx - used)`
- 营养缺口分**必须是二次曲线**:线性时周一也有 +1.4,足以让唯一能在非采购日上的冷冻龙利鱼稳赢,结果每周一必是龙利鱼。周初不加压,鲜鱼才等得到周三/周日

页面顶部的「本周体检」条实时显示这些约束的达成情况,超标/缺口标暖色。**它只是仪表盘,不兼作筛选器**——点进去挑菜不是这一行该干的事。

菜单页的交互克制到三样:今天卡片(打开就看见今天做什么)、重新排一周、每道菜的换一换。**破坏性操作一律「先做,再给撤销」,不做二次确认**——重排点了就排,顶上出现撤销(连采购勾选一起还原);换一换换完就地出现撤销。顺带记死一条:任何确认/提示都不能用 `confirm()`,嵌在 iframe 里会被沙箱拦掉,表现是「点了没反应」。菜单过期 ≥7 天时顶部提示。

## 采购批次

每批从**采购当天**管起,这样不隔夜的鱼虾能排在采购当天吃;采购日与覆盖范围全写在 `config.shopping.batch_coverage`,算法读它、不写死,改成几次采购都不用动代码。耐储类(batch_priority 3)按全周用量并入第一批。

当前节奏:**一周三次,周一/周三/周五**(2-2-3 天)。改这个只需动 config 两行,但要连带更新本节与 README 的描述。

> 选它的依据(300 周模拟):对比原来的「周日+周三」两次,生鲜在冰箱的停留从 1.27 天降到 0.70 天(最长 3 天 → 2 天),每周能排的不隔夜鲜货菜从 3.15 道升到 3.97 道,三个采购日的鲜货分布几乎持平(133/134/131%)。同样是三次,周一/周四/周六只有 3.19 道——间隔 3 天的那两批鲜货排不进去。**间隔均匀比次数多更重要。**

## 今晚吃什么(页面第二个标签)

**定位:不替代周菜单,是「今天临时不想吃排好的那道」时的逃生口。**把「从 77 道里挑」压缩成「从 3 个里挑」——一次只给 3 个候选,决策成本固定。

这一页是照三个国外 app 的长处做的,借的是机制不是形式:

| 来源 | 借的 |
|---|---|
| Mealime | 系统先决定、你只做「采纳/换一批」,不给全库列表 |
| Solid Starts | 宝宝信息跟着每道菜走——卡片背面就是这道菜的分菜时机 |
| Plan to Eat | 结果能带走:选中写进菜单、买菜清单跟着变、清单可复制给另一位掌勺 |

- 候选复用排菜那套硬约束:不隔夜的鲜货只在采购当天出现、本周已排过的不再出现、汤不作主菜、条件槽(辣度/耗时/荤素)作临时过滤
- 评分偏向当季 + 快手 + 宝宝可吃 + 用得上同批次已买的食材,取前段加权随机
- **三个必须类别不同**(荤/素/主食),且在各类别候选里随机取——只取分最高的会导致每次给的都是同几道
- 卡片可翻面看宝宝分菜时机;选中写入今天晚餐对应位置,菜单页就地可「撤销」
- **不设每日次数上限。**曾经加过「每天限 3 次重抽」并美其名曰保护决策价值,那是把需求当成了字面意义的「抽卡」——游戏化外壳对做饭这件事只是添堵。参考别的产品时借机制,不要连它的包装一起抄。

## 时令指南(页面第四个标签)

月份选择器(12 个月 + **全年**)+ 食材全年上市热力条 + 可做菜谱清单。数据全部来自 `ingredients.season_months` 与菜谱的时令交集,不需要单独维护。

界面层次固定为**月份 → 食材/菜谱 → 品类 → 属性**,筛选按标签页分开、不混排:

- 食材页:品类(叶菜/瓜茄/水果/水产…)+「宝宝能吃 / 当天吃」;每项名下显示储存方式与该吃完的期限,到手先吃的标暖色
- 菜谱页:类别(荤/半荤/素/小菜/汤/主食)+「宝宝能吃 / 不辣 / 30 分内 / 当天吃」;**点菜名展开**主料与克重、辅料、宝宝分菜时机、`steps_brief`
- 切换标签页时清空品类与属性筛选(两边的品类不是一套东西)
- 「全年」用来看整个库有什么——只按当月过滤时,淡季看起来像是「库里没有」,这是设计上的坑

菜谱展开区遵守核心原则 3:不写教程式步骤,只给决策信息;`steps_brief` 为空时明说「家常做法按你平时的来」,而不是留白。

食材库按华东全年品类维护,不能只有当季那几样:秋冬叶菜/根茎、四季水果都要在库,否则时令页到了淡季就是空的。

## 月度时令更新

每月 1 次:检索下月本地(config.location)应季蔬果(信源:农业上市数据、权威营养媒体,不依赖单一来源)→ 更新 ingredients.json 的 season_months 与新增食材 → 重新构建。此流程沉淀为 skill。

## 开发与部署

- 改完必跑校验与构建;报错找根因,不注释绕过
- 密钥不进代码(本项目应无密钥需求,出现即设计错误)
- commit message 英文简洁;git push 等用户明确指示
- 分支流:日常改动提交到 `dev`,验证通过后合并到 `main` 发布;不直接在 `main` 上提交(详见 SOP.md 第一节)
- 构建:`node scripts/build.js`(校验 + 生成 docs/index.html);量产时每批跑 `node scripts/build.js --validate`
- 页面机制:排菜算法在浏览器端运行,localStorage 记住本周菜单与冷却历史;build.js 只负责校验和数据注入。**docs/index.html 是构建产物,禁止手改,改 app/template.html 后重新构建**
- 环境:`dev` → artifact 私有预览链接(`node scripts/artifact.js` 后由 Claude 发布,URL 见 SOP.md),`main` → 公开正式站
- 部署:GitHub Pages 发布源 = `main` 分支 `/docs`(Pages 只支持根目录或 /docs,不支持 /app)。合并到 `main` 后 Pages 自动更新,1-2 分钟生效;回滚用 `git revert`
